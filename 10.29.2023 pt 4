#include <iostream>
#include <vector>
using namespace std;

int main() {
   int vecVals;
   int i;

   cin >> vecVals;

   // Creates a vector of size vecVals and initializes all values to 0
   vector<int> dailyScores(vecVals);

   for (i = dailyScores.size() - 1; i >= 0; --i){
      cin >> dailyScores.at(i);
   }
   
   for (i = 0; i < dailyScores.size(); ++i) {
      cout << dailyScores.at(i) << " ";
   }

   return 0;
}
